#  Portfolio

This is my personal portfolio web project, built with [Nuxt 3](https://nuxt.com/) and [Tailwind CSS](https://tailwindcss.com/).
It showcases my projects, skills, and experience as a web developer.

### ✏️ Recommended IDE Setup

**[WebStorm](https://www.jetbrains.com/webstorm/)** or [VSCode](https://code.visualstudio.com/) + [Vue - Official](https://marketplace.visualstudio.com/items?itemName=Vue.volar) (and disable Vetur) * additionaly [Nuxtr](https://marketplace.visualstudio.com/items?itemName=Nuxtr.nuxtr-vscode)

### 👀 Version compatibilities !!!
| Tech     | Version     |
|----------|-------------|
| **Node** | `>= 18.12`  |
| **PNPM** | `>= 10.x` |

## 📌 Project Setup

### 📦 Installing dependencies
In order to run the project, install the dependencies by the following command:

```shell
pnpm install
```

### 🖥️ Local development
To run the project on a dev server, compile and hot-reload for development, run the following command:
```shell
pnpm dev
```

### 🚀 Production deployment
To build the project, type-check, compile and minify for production, run the following command:
```shell
pnpm build
```

### 🕶️ Localy preview production build
To preview the production build locally, run the following command:
```shell
pnpm preview
```

### ⚡️ Code linting with [ESLint](https://eslint.org/)
To run linting tools based on the default configuration available in `eslint.config.js`, run the following command:
```sh
pnpm lint
```

### 🎨 Code formatting
To format project files based on the default configuration available in `.prettierrc`, run the following command:
```shell
pnpm prettier
```
